


# Changelog

## 0.3.0 — 双语与触屏界面

- 新增简体中文、English 和跟随系统三种界面语言；`zh*` 系统区域使用中文，其余区域使用英文。
- 语言偏好独立保存到 `J2MELLM_UI_PREFS`，不改变档案、配置包或聊天记录格式。
- 设置、档案、模型、文件选择、提示和已知后端错误全部接入轻量 i18n；内置自定义档案名按界面语言显示，用户自定义名称保持原样。
- 触屏设备启用全屏 Canvas、输入/停止、图片/档案、更多三按钮工具栏，以及档案、设置、语言、思维链、清空、退出菜单。
- 新增触屏拖动滚动、点击命中测试、尺寸变化重排和按像素宽度省略；非触屏设备继续保留软键命令和方向键滚动。
- Manifest 与 JAD 新增 `Nokia-MIDlet-On-Screen-Keypad: no`，版本升级为 `0.3.0`，并增加 i18n 自测（共 12 组桌面自测）。
- 完成模拟器 RMS 升级/恢复夹具、完整发布构建与目标真机回归。

## 0.2.0 — 首个公开发布版本

v0.2.0 是 J2ME LLM 的第一个公开发布版本，采用 AI 辅助的 vibe coding 协作开发。

- 新增 OpenAI、DeepSeek、Kimi、自定义四个独立档案及分档案聊天历史。
- 新增 `J2MELLM_PROFILES` 版本化 RMS、主副记录恢复和 v0.1 `J2MELLM_CFG` / `J2MELLM_CHAT` 自动迁移；旧库保留。
- 新增显式触发的 `/models` 获取、增量 `data[].id` 解析和每档案 64 项缓存。
- 新增独立的请求思考“自动 / 开启 / 关闭”、effort 和自定义协议；显示折叠保持独立，并按 Kimi K3/K2.7/K2.6/K2.5 能力映射参数。
- 多模态改为每档案默认关闭，并按需加载 JSR-75 选择器与图片响应路径。
- 请求 JSON 改为两遍计数和 512 B 缓冲直写；Base64 图片不再物化到完整请求字符串。
- 新增 Canvas 布局缓存、100 ms 流式重绘合并、49,152 字符 / 32 条活动会话预算和旧预览回收。
- 新增 `.j2cfg v2` 离线生成器、CRC 校验、部分档案导入、JSR-75 导入/导出及导入后删除明文包选项。
- 导出采用时间戳文件名、同目录临时写入与回读校验；目标存在时拒绝，绝不截断旧备份。
- 图片解码前检查格式头与像素数，发送/返回预览限制为 65,536 像素；文件浏览器限制为 256 项。
- 模型档案与聊天 RMS 均采用恢复副本；空 v0.2 库仍可继续旧数据迁移；补充 11 组桌面自测及真实 MicroEmulator v0.1 迁移/主记录损坏恢复夹具。
- 网关升级为 v0.2，可代理 `POST /v1/chat/completions` 和 `GET /v1/models`。
- 新增无需源码或系统 Node.js 的 Windows x64 独立网关 ZIP；BAT 使用包内运行时，并生成方便 J2ME 输入的 12 位纯数字设备令牌。
- MicroEmulator 启动脚本自动使用临时 ASCII 盘符，兼容位于中文路径下的工程。
- 新增完整用户、架构、开发、更新恢复和配置包中文文档。

## 0.1.0 — 未公开原型

此版本仅用于开发、构建验证和索爱 W995 实机测试，从未公开发布。

- 首个可构建、可安装版本。
- OpenAI Chat Completions 兼容请求与 SSE 流式响应。
- 文字聊天、思考内容、RMS 配置和聊天历史。
- 可选 JSR-75 图片选择、Base64 vision 输入和兼容图片预览。
- Node.js HTTPS/TLS 兼容网关。
- MicroEmulator、本地构建脚本与自测。
